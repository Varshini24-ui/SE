{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "./",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "jspdf": "^2.5.1",
    "html2canvas": "^1.4.1",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0",
    "@babel/preset-env": "^7.20.0",
    "@babel/preset-react": "^7.18.0",
    "babel-jest": "^29.3.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/user-event": "^14.4.3",
    "jest": "^29.3.0",
    "jest-environment-jsdom": "^29.3.0",
    "identity-obj-proxy": "^3.0.0",
    "cypress": "^13.0.0",
    "@cypress/webpack-dev-server": "^1.8.4",
    "eslint": "^8.40.0",
    "eslint-config-react-app": "^7.0.1",
    "prettier": "^2.8.8",
    "codecov": "^3.8.3",
    "wait-on": "^6.0.1",
    "start-server-and-test": "^2.0.3",
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:unit": "jest test/unit --verbose",
    "test:integration": "jest test/integration --verbose",
    "test:performance": "jest test/performance --testTimeout=30000 --verbose",
    "test:security": "jest test/security --verbose",
    "test:accessibility": "jest test/accessibility --verbose",
    "test:system": "jest --testPathPattern=test/system --runInBand --verbose",
    "test:system:watch": "jest --testPathPattern=test/system --watch",
    "test:system:coverage": "jest --testPathPattern=test/system --coverage --runInBand",
    "test:system:verbose": "jest --testPathPattern=test/system --verbose --runInBand --detectOpenHandles",
    "test:system:scenarios": "jest test/system/scenarios --runInBand --verbose",
    "test:system:e2e": "jest test/system/scenarios/endToEndUserFlow.test.js --runInBand --verbose",
    "test:system:resume": "jest test/system/scenarios/resumeAnalysisWorkflow.test.js --runInBand --verbose",
    "test:system:auth": "jest test/system/scenarios/authenticationFlow.test.js --runInBand --verbose",
    "test:system:chatbot": "jest test/system/scenarios/chatbotInteraction.test.js --runInBand --verbose",
    "test:debug": "node --inspect-brk node_modules/.bin/jest --runInBand",
    "test:all": "jest --coverage && npm run cypress:run",
    "test:ci": "jest --ci --coverage --maxWorkers=2",
    "test:full": "npm run test:coverage && npm run test:e2e && npm run test:system:coverage",
    "test:complete": "npm run test:unit && npm run test:integration && npm run test:system && npm run test:performance && npm run test:security && npm run test:accessibility",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:headless": "cypress run --headless --browser chrome",
    "cypress:chrome": "cypress run --browser chrome",
    "cypress:firefox": "cypress run --browser firefox",
    "cypress:edge": "cypress run --browser edge",
    "test:e2e": "start-server-and-test start http://localhost:3000 cypress:run",
    "test:e2e:open": "start-server-and-test start http://localhost:3000 cypress:open",
    "test:e2e:headless": "start-server-and-test start http://localhost:3000 cypress:headless",
    "test:e2e:chrome": "start-server-and-test start http://localhost:3000 cypress:chrome",
    "test:e2e:ci": "start-server-and-test start http://localhost:3000 'cypress run --record'",
    "cypress:verify": "cypress verify",
    "cypress:info": "cypress info",
    "cypress:version": "cypress version",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write src/ test/ cypress/",
    "format:check": "prettier --check src/ test/ cypress/",
    "coverage:report": "open coverage/lcov-report/index.html || xdg-open coverage/lcov-report/index.html",
    "coverage:system": "jest --testPathPattern=test/system --coverage && npm run coverage:report",
    "clean": "rm -rf node_modules cypress/videos cypress/screenshots coverage",
    "clean:cypress": "rm -rf cypress/videos cypress/screenshots",
    "clean:coverage": "rm -rf coverage",
    "clean:system": "rm -rf test/system/reports/*",
    "postinstall": "cypress verify || echo 'Cypress verification failed'"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFilesAfterEnv": [
      "<rootDir>/setupTests.js"
    ],
    "moduleNameMapper": {
      "\\.(css|less|scss|sass)$": "identity-obj-proxy",
      "\\.(jpg|jpeg|png|gif|svg)$": "<rootDir>/__mocks__/fileMock.js"
    },
    "collectCoverageFrom": [
      "src/**/*.{js,jsx}",
      "!src/index.js",
      "!src/reportWebVitals.js"
    ],
    "coverageThreshold": {
      "global": {
        "branches": 50,
        "functions": 50,
        "lines": 50,
        "statements": 50
      }
    },
    "testTimeout": 30000,
    "testMatch": [
      "**/__tests__/**/*.[jt]s?(x)",
      "**/?(*.)+(spec|test).[jt]s?(x)"
    ],
    "coveragePathIgnorePatterns": [
      "/node_modules/",
      "/test/fixtures/",
      "/test/helpers/",
      "/test/__mocks__/"
    ]
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}